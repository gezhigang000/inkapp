# 微信公众号 API 配置
# 复制此文件为 config.env 并填入真实值
# cp config.env.example config.env

# 在微信公众平台 -> 开发 -> 基本配置 中获取
WECHAT_APP_ID=你的AppID
WECHAT_APP_SECRET=你的AppSecret

# 文章配置
AUTHOR=AI爱好者
OUTPUT_DIR=/path/to/output/articles

# 封面图路径（可选，留空则不设置封面）
COVER_IMAGE=

# 发布模式: draft=仅存草稿(推荐), publish=直接发布
PUBLISH_MODE=draft

# AI 图片生成 API Key（可选，用于文章配图兜底）
OPENAI_API_KEY=

# ============================================================
# 大模型配置
# ============================================================
# LLM 提供商: claude (默认) / deepseek / openai
LLM_PROVIDER=claude

# DeepSeek 配置（LLM_PROVIDER=deepseek 时需要）
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat

# OpenAI 模型（LLM_PROVIDER=openai 时使用，API Key 复用上面的 OPENAI_API_KEY）
OPENAI_MODEL=gpt-4o

# ============================================================
# 搜索配置（仅 LLM_PROVIDER 非 claude 时生效）
# ============================================================
# 搜索提供商: tavily (推荐) / serpapi
SEARCH_PROVIDER=tavily
TAVILY_API_KEY=
SERPAPI_API_KEY=
